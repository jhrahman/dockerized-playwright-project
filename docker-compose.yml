services:
  playwright:
    image: playwright-dev

    build:
      context: .
      dockerfile: Dockerfile.dev

    container_name: playwright-dev-container

    volumes:
      - .:/app

    tty: true